# Define a list of names
names = ["Jake", "Zac", "Ian", "Ron", "Sam", "Dave"]
# Let's search for "Sam" in the list
search_name = "Sam"
# Check if the name "Sam" is in the list
if search_name in names:
    print(f"Found {search_name} listed!")
else:
    print(f"{search_name} no name in the list.")